{
  "name": "phage-lol-root",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "build": "npm run build:client",
    "build:client": "cd client && npm ci --loglevel=error --no-fund --no-audit && npm run build",
    "start": "node server/server.js",
    "dev": "concurrently \"npm run start\" \"cd client && npm run dev\"",
    "smoke": "node scripts/playtest_smoke.cjs",
    "test": "cd client && npm run test && cd ../server && npm run test",
    "test:client": "cd client && npm run test",
    "install-client": "cd client && npm install",
    "install-server": "cd server && npm install",
    "postinstall": "npm run install-client && npm run install-server"
  },
  "dependencies": {
    "@msgpack/msgpack": "^3.1.3",
    "express": "^4.18.2",
    "socket.io": "^4.7.4",
    "socket.io-client": "^4.8.3"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "jsdom": "^29.1.0"
  }
}
